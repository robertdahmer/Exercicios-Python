import math
num = float(input('Digite um número real qualquer: '))
print('O número digitado ({}) arredondado para cima fica: {}'.format(num, math.ceil(num)))
print('O número digitado ({}) arredondado para baixo fica: {}'.format(num, math.floor(num)))
